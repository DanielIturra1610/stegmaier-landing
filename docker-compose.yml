version: '3'

services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
      - VITE_HOST=0.0.0.0
    command: sh -c "npm install && npm run dev -- --host"